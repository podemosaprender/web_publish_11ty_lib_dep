<h1 class="text-center"><strong><a href="{{page.url}}"><img class="img-fluid rounded-circle" alt="{{metadata.description}}" src="img/icon.png" width="40" height="40" /> {{ metadata.title }}</a></strong></h1>
<h2>{{ metadata.description }}</h2>
{% set maxPosts = collections.posts.length | min(4) %}
{% set postslist = collections.posts | head(-4) %}
{% set postslistCounter = collections.posts | length %}
